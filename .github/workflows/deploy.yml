# name: Build and Deploy to DockerHub

# on:
#   push:
#     branches:
#       - main
# jobs:
#     build-and-push:
#         runs-on: ubuntu-latest
#         steps:
#             - name: Checkout code
#               uses: actions/checkout@v2

#             - name: Login to DockerHub
#               uses: docker/login-action@v1
#               with:
#                 username: ${{ secrets.DOCKER_USERNAME }}
#                 password: ${{ secrets.DOCKER_PASSWORD }}

#             - name: Build and Push Docker image
#               uses: docker/build-push-action@v2
#               with:
#                 context: .
#                 file: ./docker/Dockerfile.user-app
#                 push: true
#                 tags: ${{ secrets.DOCKER_USERNAME }}/paytm:latest

#             - name: Verify Pushed Image
#               run: docker pull ${{ secrets.DOCKER_USERNAME }}/paytm:latest

#             - name: Deploy to EC2
#               uses: appleboy/ssh-action@master
#               with: 
#                 host: ${{ secrets.SSH_HOST }}
#                 username: ${{ secrets.SSH_USERNAME }}
#                 key: ${{ secrets.SSH_KEY }}
#                 script: |
#                   sudo docker pull ${{ secrets.DOCKER_USERNAME }}/paytm:latest
#                   sudo docker stop paytm || true
#                   sudo docker rm paytm || true
#                   sudo docker run -d -p 3000:3000 --name paytm ${{ secrets.DOCKER_USERNAME }}/paytm:latest

